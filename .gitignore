/target
/target/swift
**/*.rs.bk

# iOS: the project is generated from ios/project.yml, never checked in.
/ios/*.xcodeproj
/ios/*.xcworkspace
/ios/Generated
/ios/build
*.xcframework

# Machine-specific: local SOCKS proxy for cargo. Not portable.
/.cargo/config.toml

# UniFFI output, produced by the build.
/ios/Generated

# Pinned XcodeGen, built once by scripts/xcodegen-bin.sh. The macOS job puts it
# outside target/, which Swatinem/rust-cache prunes as its own.
/target/xcodegen
/.xcodegen

# cargo-mutants output. Regenerated on every run, and its lock.json records the
# hostname and username of whoever ran it.
/mutants.out
/mutants.out.old
/mutants.out.*
