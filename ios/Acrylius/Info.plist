<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Supplying INFOPLIST_FILE means Xcode generates nothing, so every key a
         bundle needs has to be here. Omitting CFBundleIdentifier in particular
         produces a bundle that fails to build or refuses to install, with a
         message that does not obviously point back at this file. -->
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>CFBundleName</key>
    <string>$(PRODUCT_NAME)</string>
    <key>CFBundlePackageType</key>
    <string>APPL</string>
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>
    <key>CFBundleDevelopmentRegion</key>
    <string>en</string>
    <key>CFBundleDisplayName</key>
    <string>Acrylius</string>
    <key>CFBundleShortVersionString</key>
    <string>0.1.0</string>
    <key>CFBundleVersion</key>
    <string>1</string>

    <key>LSRequiresIPhoneOS</key>
    <true/>
    <key>UILaunchScreen</key>
    <dict/>
    <key>UIApplicationSupportsIndirectInputEvents</key>
    <true/>
    <!-- Orientation is set per device family, using the ~ipad suffix iOS
         resolves at launch.

         The phone is portrait only. It is a companion you pick up to lock a
         machine or send a clipboard, and there is nothing here that reads
         better wide.

         The iPad has to offer all four. An iPad app that does not opt out of
         multitasking must support every orientation, because it can be resized
         into a slide-over or split view at any moment; declaring fewer is what
         produced the "All interface orientations must be supported unless the
         app requires full screen" warning. The alternative would be
         UIRequiresFullScreen, which buys nothing and costs multitasking. -->
    <key>UISupportedInterfaceOrientations</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
    </array>
    <key>UISupportedInterfaceOrientations~ipad</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationPortraitUpsideDown</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>
    <key>UIRequiredDeviceCapabilities</key>
    <array>
        <string>arm64</string>
    </array>

    <!-- Browsing for _acrylius._tcp goes through the system mDNSResponder, so
         it needs no entitlement, unlike UDP broadcast, which a free developer
         account cannot have. Both keys are required or the browse silently
         finds nothing. -->
    <key>NSLocalNetworkUsageDescription</key>
    <string>Acrylius finds your PC on this network. It never leaves your network.</string>
    <key>NSBonjourServices</key>
    <array>
        <string>_acrylius._tcp</string>
    </array>

    <key>NSFaceIDUsageDescription</key>
    <string>Face ID confirms it is you before unlocking your PC.</string>

    <!-- Raw NWConnection is not subject to App Transport Security, which
         governs URLSession and CFNetwork; no ATS exception is needed. -->
    <key>ITSAppUsesNonExemptEncryption</key>
    <false/>
</dict>
</plist>
